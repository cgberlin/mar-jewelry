import React, { Component, PropTypes } from 'react'
import { connect } from 'react-redux'
import store from '../index'

const HomePage = () => {
	return (
		<div>
			<img src = '../src/images/niche_award.png' alt = "image of greg getting the niche award"/>
		</div>
		);
}

export default HomePage;